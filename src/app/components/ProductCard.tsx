const ProductCard = () => {
  return (
    <div>
      <h1>ProductCardProductCard</h1>
      <button onClick={() => console.log("Click@@!!!")}>Add to Cart</button>
    </div>
  );
};

export default ProductCard;
